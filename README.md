# MCNE Student Portal

Desktop and mobile demo for a Biology lesson: read the PDF, watch the clip, then answer. An admin strip shows course dwell time, PDF pages, and video time.

```bash
npm install
npm run dev
```

Build for Vercel with `npm run build`. No database or environment variables are required.
